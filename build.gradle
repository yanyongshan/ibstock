import com.sun.org.apache.xalan.internal.xsltc.cmdline.Compile

apply plugin: 'java'
apply plugin: 'application'
mainClassName = 'com.stock.ibapi.Main'

repositories {
    mavenLocal()
    mavenCentral()
}

jar {
    baseName = 'ibstock'
    version =  '1.0'
}

//dependencies {
//    compile(
//            'org.apache.commons:commons-cli:1.3.1',
//    )
//}

task wrapper(type: Wrapper) {
    gradleVersion = '2.1'
}

tasks.withType(Compile) {
    options.encoding = "UTF-8"
}